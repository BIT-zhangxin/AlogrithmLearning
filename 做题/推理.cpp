//#include <stdio.h>////#define LSON 1,m,x<<1//#define RSON m+1,r,x<<1|1//const int MAX = 1e5 + 50;//int a[MAX], xds[4 * MAX];////int max(int a, int b)//{//	return (a > b) ? a : b;//}////void Pushup(int x) {//	xds[x] = max(xds[x << 1], xds[x << 1 | 1]);//}////void Js(int l, int r, int x) {//	if (l == r) {//		xds[x] = a[l];//		return;//	}//	int m = (l + r) / 2;//	Js(LSON);//	Js(RSON);//	Pushup(x);//}//////int main()//{//	for (int i = 0; i < 10; i++)//	{//		a[i] = i;//	}//	Js(0, 9, 0);//	for (int i = 0; i < 40; i++)//	{//		printf("%d", xds[i]);//		if ((i + 1) % 4 == 0) putchar(10);//		else putchar(' ');//	}//	return 0;//}